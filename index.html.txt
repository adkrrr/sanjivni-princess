<!DOCTYPE html>
<html>
<head>
    <title>For My Princess 👑</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            background: linear-gradient(135deg, #fbc2eb, #a6c1ee);
            font-family: 'Georgia', serif;
            text-align: center;
            color: white;
            overflow: hidden;
        }

        h1 {
            margin-top: 80px;
            font-size: 3rem;
            text-shadow: 2px 2px 10px rgba(0,0,0,0.3);
            animation: fadeIn 2s ease-in-out;
        }

        p {
            font-size: 1.4rem;
            margin: 20px 40px;
            animation: fadeIn 3s ease-in-out;
        }

        .buttons {
            margin-top: 40px;
        }

        button {
            padding: 15px 35px;
            font-size: 1.2rem;
            border: none;
            border-radius: 30px;
            margin: 10px;
            cursor: pointer;
            transition: 0.3s;
        }

        #yes {
            background-color: #ff69b4;
            color: white;
        }

        #yes:hover {
            background-color: #ff1493;
            transform: scale(1.1);
        }

        #no {
            background-color: white;
            color: #ff69b4;
            position: absolute;
        }

        @keyframes fadeIn {
            from {opacity: 0;}
            to {opacity: 1;}
        }

        .heart {
            position: absolute;
            color: pink;
            font-size: 20px;
            animation: float 5s linear infinite;
        }

        @keyframes float {
            from {transform: translateY(100vh);}
            to {transform: translateY(-10vh);}
        }
    </style>
</head>
<body>

    <h1>👑 My Princess Sanjivni Mumma 👑</h1>
    <p>You are the most precious blessing in my life.</p>
    <p>Your smile feels like magic ✨</p>
    <p>Your presence feels like home 🏰</p>
    <p><strong>Will you be my princess? 💖</strong></p>

    <div class="buttons">
        <button id="yes" onclick="yesClicked()">YES MY KING 💕</button>
        <button id="no" onmouseover="moveButton()">NO 😜</button>
    </div>

    <script>
        function yesClicked() {
            document.body.innerHTML = `
                <h1>😭👑 SHE SAID YES 👑😭</h1>
                <p>I promise to treat you like the princess you are, Sanjivni Mumma 💖</p>
                <p>This is just the beginning of our fairytale ✨🏰</p>
            `;
        }

        function moveButton() {
            var button = document.getElementById("no");
            var x = Math.random() * window.innerWidth;
            var y = Math.random() * window.innerHeight;
            button.style.left = x + "px";
            button.style.top = y + "px";
        }

        // Floating hearts
        setInterval(function() {
            var heart = document.createElement("div");
            heart.className = "heart";
            heart.innerHTML = "💖";
            heart.style.left = Math.random() * window.innerWidth + "px";
            heart.style.fontSize = (Math.random() * 20 + 10) + "px";
            document.body.appendChild(heart);

            setTimeout(function() {
                heart.remove();
            }, 5000);
        }, 500);
    </script>

</body>
</html>